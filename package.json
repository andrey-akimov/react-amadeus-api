{
	"name": "hotels",
	"version": "0.1.0",
	"private": true,
	"main": "index.js",
	"engines": {
		"node": "9.3.0",
		"npm": "5.5.1"
	},
	"dependencies": {
		"body-parser": "^1.18.2",
		"dotenv": "^5.0.1",
		"express": "^4.16.3"
	},
	"scripts": {
		"start": "node server.js",
		"heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
	}
}
